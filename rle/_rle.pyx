# cython: language_level=3
# distutils: language=c++


cdef extern from "decode.hpp":
    pass
    #cdef string Decode(
    #    char *inArray,
    #    char *outArray,
    #    int inLength,
    #    int outLength,
    #)
